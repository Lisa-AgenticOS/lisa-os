[package]
name = "bus-tools"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
anyhow = "1.0.104"
forge-harness = { version = "0.1.0", path = "../forge-harness" }
serde_json = "1.0.151"
zbus = { version = "5.18.0", features = ["p2p", "tokio", "blocking-api"], default-features = false }
