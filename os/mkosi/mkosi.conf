# Lisa OS image profile (Track I, ADR-0001). M0 target: boots in QEMU via
# `just vm` with A/B update + rollback demonstrated (PLAN §10).
#
# Build on Linux only: `mkosi --directory os/mkosi build` (CI's job —
# .github/workflows/nightly.yml). The pinned Arch snapshot mirror
# (os/repo-tools) replaces the default mirrors before beta.

[Distribution]
Distribution=arch

[Build]
# Bootstrap a pacman-capable tools tree so the image builds on any host
# distro (CI runs on Ubuntu runners).
ToolsTree=default

[Output]
Format=disk
ImageId=lisa
# Baked into /usr/lib/os-release as IMAGE_VERSION= (release.yml rewrites the
# "1" to the real YYYYMMDD.run version). systemd-sysupdate reads this to
# determine the installed version; without it, `lisa update --reboot` errors
# with "/etc/os-release lacks IMAGE_VERSION= field" and never reboots.
ImageVersion=1
Output=lisa

[Content]
Bootable=yes
Hostname=lisa
# Bake timezone/locale/keymap so systemd-firstboot has nothing to prompt.
# Without these, first boot stops at an interactive "select timezone"
# question on the console before gdm — a default of the image, not
# something the user set. These are changeable later in GNOME Settings ›
# Date & Time / Region (or edit these lines + rebuild). Timezone follows
# the field device's locale (Europe/Tirane); UTC is the neutral fallback.
Timezone=Europe/Tirane
Locale=en_US.UTF-8
Keymap=us
# Find root deterministically by the GPT partition label repart sets
# (Label=root_1; release builds bump it to root_<ver> and keep this in
# sync). gpt-auto-root is unreliable on real hardware with other disks
# present — an explicit PARTLABEL always resolves. `rw` for btrfs.
# Boot splash (PLAN §3/§6): `quiet splash` engages Plymouth and keeps the
# framebuffer free of kernel/unit text — the Mac shows the Apple logo,
# then the Lisa splash, then the session. No scrolling console. The theme
# is Arch's stock `bgrt` carrying our watermark, pinned in
# mkosi.extra/etc/plymouth/plymouthd.conf and its initrd twin; this
# comment said "the `lisa` theme, violet + wordmark" for a week after
# that theme was deleted (1fec591). bgrt's background is 0x000000 and
# this Mac has no ACPI BGRT image, so the splash IS a black field with a
# spinner and the wordmark — see README "What the boot actually looks
# like" before reading black as broken. `console=ttyS0` routes ALL kernel/systemd output to
# the serial line instead of tty0, so (a) the framebuffer stays clean for
# Plymouth on the real display, and (b) CI keeps a serial log — though the
# CI boot-check supplies its OWN `-append` (nightly.yml/release.yml) and
# never reads this line, so it is unaffected either way. A boot hang is no
# longer photographable on-screen; it is instead captured by
# lisa-boot-report.service dumping the journal to the ESP (postinst).
# Plymouth degrades to a blank/text screen if it fails — it never blocks
# boot.
# systemd.gpt_auto=no: gpt-auto's fstab-defer races the fstab-generator, and
# when gpt-auto wins it mounts /var by a machine-id-derived UUID no partition
# has → emergency mode on updated slots (ADR-0018 forensics). Everything it
# provided is explicit: root= here, /var + /home + /efi in the baked fstab.
# `plymouth.ignore-serial-consoles` is why there is a splash at all
# (issue #45), and it is not cosmetic.
#
# Plymouth refuses to build a graphical renderer when it finds a serial
# console, on the reasoning that a headless server wants scrolling text
# on the wire instead. Its own debug log, on the reference iMac:
#
#     console /dev/ttyS0 found!
#     serial consoles detected, managing them with details forced
#     creating devices for  (renderer type: 4294967295)
#
# 4294967295 is 0xFFFFFFFF — no renderer. `console=ttyS0` is here for
# CI, which reads the boot over the serial line, and it silently cost
# every real machine its splash. This flag keeps the console (CI still
# greps "Welcome to") and tells Plymouth to draw anyway.
#
# Adding `console=tty0` was tried first and is NOT the fix: Plymouth
# forces details mode when it finds ANY serial console, so ttyS0 still
# triggered it — and moving /dev/console to the display merely made the
# boot text visible where the black screen had been.
KernelCommandLine=root=PARTLABEL=root_1 rw quiet splash console=ttyS0 systemd.gpt_auto=no plymouth.ignore-serial-consoles
# Initrd must mount btrfs root AND drive input on bare metal — without
# USB HID here, a rescue prompt has no keyboard (Apple wired keyboards
# included). Broad USB/HID + storage set keeps unknown hardware bootable.
# The option name matters: the current mkosi INI name is
# KernelInitrdModules= (CLI --kernel-modules-initrd-include=). The old
# KernelModulesInitrdInclude= spelling produced initrds with ZERO
# modules on v26-v29 — every USB boot dead (issue #46). These modules
# ride the extra kernel-modules initrd mkosi appends at UKI assembly.
KernelInitrdModules=
        btrfs
        usbhid
        hid_generic
        hid_apple
        xhci_pci
        xhci_hcd
        ehci_pci
        uhci_hcd
        usb_storage
        uas
        sd_mod
        vfat
        nvme
        ahci
        # --- Display for the early Plymouth splash (ADR-0017/ADR-0025) ---
        # The native GPU driver + its firmware, in the initrd — the way
        # Ubuntu gets a splash at native resolution the instant the
        # firmware logo goes away. ADR-0017 shipped simpledrm only and the
        # field iMac18,2 (Radeon Pro 560, Polaris11/Baffin) still showed a
        # BLACK screen from the Apple logo to GDM on every boot.
        #
        # mkosi resolves the module *and firmware* dependencies of every
        # module matched here (mkosi/kmod.py resolve_module_dependencies +
        # filter_firmware; mkosi.1 "Firmware dependencies of kernel modules
        # installed in the image are automatically included"), so amdgpu
        # drags in its modinfo depends= set (drm_display_helper, ttm,
        # amdxcp, gpu-sched, drm_exec, drm_buddy, drm_ttm_helper,
        # drm_suballoc_helper, drm_panel_backlight_quirks, cec, video,
        # i2c-algo-bit) and the amdgpu/*.bin blobs it declares. Nothing
        # else needs listing, and FirmwareFiles= is deliberately NOT set:
        # that setting also drives the image-wide firmware pruning in
        # mkosi's run_depmod(), which would delete every firmware file no
        # module declares — e.g. the brcm/*.hcd Bluetooth blobs (btbcm and
        # btusb declare zero MODULE_FIRMWARE), silently breaking the
        # iMac's Bluetooth. Trading ~36 MB of ESP for that is not a trade.
        amdgpu
        # Same job in QEMU, where the display is `-device virtio-vga`
        # (screenshots.yml): give Plymouth a real DRM driver in the VM too.
        # ~50 KB, no firmware. Groundwork for issue #26.
        virtio_gpu
        # simpledrm: a NO-OP on Arch's kernel today — modules.builtin lists
        # kernel/drivers/gpu/drm/sysfb/simpledrm.ko (as it does drm.ko and
        # drm_kms_helper.ko), so this glob matches no .ko and adds nothing;
        # the EFI GOP framebuffer is already there, built in. Kept so that
        # a future kernel which modularizes it still lands it here.
        simpledrm
# --- The default initrd's own content (issue #50, ADR-0028) ---
# mkosi 26 builds the default initrd as an internal sub-image configured
# ONLY from its bundled mkosi-initrd resources plus the settings below
# (mkosi/config.py:finalize_default_initrd). A `mkosi.initrd/` directory
# next to this file is read by NOTHING — the one this repo carried was
# inert from the day it was written, which is why ADR-0017's splash
# config and ADR-0022's rescue resolver never shipped. These are the
# knobs that actually reach that sub-image; extra *files* go through
# mkosi.finalize + initrd-overlay/ (see that script).
#
# InitrdProfiles=plymouth is mkosi's own purpose-built switch for a
# graphical initrd (mkosi/resources/mkosi-initrd/mkosi.profiles/plymouth
# — it adds Packages=plymouth). It replaces the dead
# `mkosi.initrd/mkosi.conf` that meant to do this. The theme,
# the watermark and the plymouth-start symlink ride in via the overlay,
# so this is never the theme-less flash ADR-0017 set out to avoid.
# `util-linux` (sfdisk, for the lisa-loader-disk udev helper) needs no
# line here: mkosi's own Arch drop-in already installs it in the initrd.
InitrdProfiles=plymouth
# The kernel package is per-architecture (Packages= is append-only across
# drop-ins, so the arch-specific name cannot live here): `linux` on x86_64,
# `linux-aarch64` on Arch Linux ARM — see mkosi.conf.d/ (ADR-0021).
Packages=
        base
        linux-firmware
        # Arch splits linux-firmware into per-vendor subpackages; name the
        # ones field hardware needs so a change in the meta package's
        # dependency set can never silently drop them. First field target
        # is an iMac18,2: Radeon Pro 560 (amdgpu — no blob, no display)
        # and BCM43602 Wi-Fi (brcmfmac).
        linux-firmware-amdgpu
        linux-firmware-broadcom
        systemd
        systemd-ukify
        # The download half of systemd-sysupdate. `systemd-pull` does not
        # link libcurl — it dlopen()s "libcurl.so.4" at runtime and turns
        # ANY dlopen failure into EOPNOTSUPP (systemd
        # src/basic/dlfcn-util.c:dlopen_verbose →
        # src/shared/curl-util.c:curl_glue_new → src/import/pull.c
        # raw_pull_new), which surfaces as exactly
        # "Failed to allocate puller: Operation not supported" — issue #45's
        # signature. Arch ships libcurl only as an OPTIONAL dependency of
        # systemd ("curl: systemd-journal-upload, machinectl pull-tar and
        # pull-raw"), so it lands in the image today purely because
        # networkmanager happens to depend on it. The ability of the OS to
        # update itself must not ride on an unrelated package's dependency
        # list; name it here and let the nightly assert it.
        curl
        # TLS trust store for the HTTPS release channel the shipped
        # /usr/lib/sysupdate.d/*.transfer files pull from. A curl dependency
        # today — declared for the same reason.
        ca-certificates
        # Graphical boot splash (PLAN §3/§6). Ships plymouthd + the
        # plymouth-quit*/read-write units that hand the framebuffer to GDM,
        # and renders Arch's stock `bgrt` theme carrying our watermark. Present
        # in the rooted system; plymouth-start is wired into sysinit.target
        # via mkosi.extra so the splash comes up even though mkosi's own
        # initrd does not carry Plymouth (see README "Boot splash").
        plymouth
        btrfs-progs
        iwd
        # Bluetooth stack + bluetoothctl for pairing Apple Magic input
        # devices (USB HID needs nothing extra; hid_apple is in-kernel).
        bluez
        bluez-utils
        sudo
        vim
        # SSH server: a headless field device must be reachable without a
        # keyboard-at-the-console. Enabled by the preset; password auth
        # against the provisional lisa/lisa account until the M7 OOBE and
        # key-based access land.
        openssh
        # --- Rootless container runtime (issue #130 phase 0, ADR-0034).
        # `lisa dev install mysql` runs pacman inside a rootless container
        # in the user's home, because the root is immutable and the next
        # A/B update replaces it wholesale. The runtime has to be in the
        # image: it is the thing that would otherwise need `sudo` to
        # install, and `escalate.privilege` is an unoverridable Deny in
        # our own guard (CLAUDE.md 7b).
        #
        # Weighed against ADR-0023 phase 3 before adding, not after: the
        # populated root measures 4542 MiB against a 7 GiB slot target, so
        # this set is affordable. The delta it actually costs is recorded
        # in #130 from the first release that carries it — measured, in
        # the tradition of the correction ADR-0023 had to publish about
        # its own estimate.
        #
        # Named individually because podman lists all of these as
        # OPTIONAL dependencies, and rootless podman without them does
        # not refuse to start — it degrades. That is the #130 phase-0
        # failure mode in one sentence.
        podman
        # OCI runtime. podman's default, and the one containers-common's
        # shipped policy expects.
        crun
        # Rootless networking: netavark is the network backend and
        # aardvark-dns its name resolver; without them a container has no
        # working DNS and the failure reads as "the package mirror is
        # down".
        netavark
        aardvark-dns
        # Unprivileged user-mode networking for the rootless case (pasta).
        passt
        # Rootless layered storage. The root is btrfs, but the container
        # store lives under $HOME (ADR-0034) and must not assume it.
        fuse-overlayfs
        # PID 1 inside the container — without an init, `podman run` leaves
        # zombies for anything that forks.
        catatonit
        # The file manager. Present on the device today only as an
        # accidental transitive dependency of the GNOME session — it
        # appears nowhere in this file, and `lisa apps`/Preview depend on
        # it: Preview implements org.gnome.NautilusPreviewer2 so Space
        # gives Quick Look (#146), which integrates with nothing if
        # Nautilus is not there.
        #
        # Third instance today of the shape ADR-0034 rule 7a names:
        # libcurl arrived only because networkmanager happened to want it
        # (#45), gst-plugins-base did NOT arrive because nothing declared
        # it, and this is the same bet on somebody else's dependency
        # graph. Declared, so an upstream reshuffle cannot quietly take
        # the file manager — and Quick Look — away.
        nautilus

        # --- Media codecs, for Surfer (ADR-0037, #146) ---------------
        # Measured on the reference device 2026-08-02: Surfer opens
        # youtube.com and renders it, and `gst-inspect-1.0 opusdec` says
        # no such element. YouTube serves VP9 or AV1 video with Opus
        # audio; the image had vp9dec (from gst-plugins-good) and no
        # audio decoder at all, so a video would have played silent at
        # best.
        #
        # Nothing here was ever declared. The plugins arrived as
        # transitive dependencies of GNOME and WebKit, which is why the
        # set is arbitrary: `gst-plugins-good` came along and brought
        # vpx, while `gst-plugins-base` did NOT — only its -libs, which
        # are libraries and register no elements. That is issue #45's
        # shape exactly ("libcurl arrived only as an accidental
        # transitive dependency"): the browser's ability to play media
        # rode on what something else happened to pull in.
        #
        # A browser Lisa ships as its own app cannot have its media
        # support decided by accident, so the codecs are named.
        # gst-plugins-base: opus, vorbis, theora — the decoders WebKit
        # asks for first.
        gst-plugins-base
        # H.264 and AAC, via ffmpeg, which the image already carries for
        # other reasons. Still the fallback half of the web.
        gst-libav
        # AV1 (dav1d, also already present as a library) and the rest of
        # the modern set YouTube negotiates before falling back.
        gst-plugins-bad
        # --- The desktop session (ADR-0048: we write the desktop, we do
        # not patch GNOME's). GNOME's *foundation* stays and is not up
        # for removal — mutter, GTK4/libadwaita, gnome-session,
        # gnome-settings-daemon, gsettings-desktop-schemas, the portals.
        # What leaves is the shell. Deliberately NOT the `gnome` group —
        # every name below earns its place.
        # Display manager; autologin config in mkosi.extra/etc/gdm.
        gdm
        # Lisa Desktop — the GNOME Shell fork (ADR-0038), built by the
        # lisa-desktop repo's own CI and pulled from the [lisa] index
        # (ADR-0039 step 4; mkosi.pkgmngr/etc/pacman.d/lisa.conf).
        #
        # THIS IS WHERE `gnome-shell` USED TO BE. The package installs at
        # /usr with provides=(gnome-shell) conflicts=(gnome-shell), so it
        # takes over /usr/bin/gnome-shell, the org.gnome.Shell.* D-Bus
        # names and the schemas — and every dependency that names
        # gnome-shell (gdm's, gnome-control-center-lisa's) resolves to
        # it. Stock GNOME Shell is NOT installed and is not a fallback:
        # the fallback is the previous A/B root slot (ADR-0001), and
        # behind that the USB image every release publishes.
        #
        # The prefix matters and is not cosmetic. The fork briefly
        # installed to a private /usr/lib/lisa-desktop so it could sit
        # beside stock gnome-shell; that arrangement set
        # GSETTINGS_SCHEMA_DIR in the shell's unit, which shadows
        # org.gnome.shell — and therefore shadowed lisa-shell's
        # 10_lisa-shell.gschema.override, the file that enables the three
        # Lisa extensions. It would have booted looking like stock GNOME
        # with none of Lisa's surfaces in it (#241's shape). At /usr the
        # override is compiled into the same schemas the shell reads.
        # (the package NAME is in the per-arch drop-ins: Packages= is
        # append-only across drop-ins, and lisa-desktop-shell is
        # x86_64-only until ADR-0021's aarch64 lane has a build behind
        # it. mkosi.conf.d/x86_64.conf and mkosi.conf.d/aarch64.conf.)
        gnome-session
        # Terminal in the desktop (§5.8: `lisa` CLI preinstalled needs
        # a terminal to live in; gnome-console is GNOME's minimal one).
        gnome-console
        # The Settings app is NOT named here any more: it is
        # lisa-desktop-control-center now (provides/conflicts
        # gnome-control-center), which only the lanes with the package
        # available can install — release.yml's 50-release.conf and the
        # aarch64 drop-in name it explicitly. Naming the stock string
        # here would make pacman install Arch's stock settings and
        # conflict with ours; naming ours here would break the nightly,
        # which has no local packages and a [lisa] index that gains the
        # new name only at the next publish. Nightly boot-check images
        # simply carry no Settings app, which they never asserted anyway.
        # The Secret Service. Without it there is no credential store on
        # the bus at all, and Settings → Online Accounts becomes a trap:
        # the account is added, appears connected, and every consumer
        # then fails with "Failed to retrieve credentials from the
        # keyring". That is what a connected Google account did on the
        # field iMac on 2026-07-30 — GOA had the IMAP host, the user
        # name and the OAuth2 interface, and no token it could hand out.
        #
        # Arch's gdm already calls pam_gnome_keyring.so from both
        # gdm-password and gdm-autologin, as `optional` — so the module
        # being absent failed silently and the whole path came back the
        # moment the package existed. Nothing else to wire.
        #
        # Autologin caveat (mkosi.extra/etc/gdm/custom.conf): no password
        # is typed at login, so PAM cannot unlock the login keyring and
        # GNOME prompts for it the first time an app wants a secret. That
        # is correct behaviour for a keyring, and it goes away with the
        # M7 OOBE when a real user logs in with a real password.
        #
        # Note for the model host: `lisa remoted` does NOT use this. It
        # keeps provider keys in its own store (daemons/remoted/secrets.rs)
        # precisely so signing in to a cloud model never depends on a
        # desktop session being unlocked.
        gnome-keyring
        # Portals are the trust boundary (§5.5); the GNOME backend +
        # the GTK fallback (file chooser et al.) make portal calls real
        # in the session instead of hanging.
        xdg-desktop-portal-gnome
        xdg-desktop-portal-gtk
        # GL/EGL drivers — mutter needs a renderer: radeonsi for the
        # iMac18,2's Radeon Pro 560, llvmpipe fallback in QEMU/CI.
        mesa
        # RADV Vulkan ICD — llama-server is built with the Vulkan
        # backend (#193) and offloads to the Radeon when an ICD is
        # present; without one it enumerates zero devices and stays on
        # CPU (which is what QEMU/CI get: deliberately no vulkan-swrast,
        # lavapipe would be slower than ggml's own CPU path).
        vulkan-radeon
        # Preview's page reordering (PLAN §5.8 slice 2): poppler-glib
        # can add annotations but cannot reorder or delete pages —
        # qpdf applies the new page order at save.
        qpdf
        # Preview's media peeks (#200) need no extra package: Arch
        # links the GStreamer media backend directly into libgtk-4
        # (ldd on the device shows libgstplay), and the codec plugins
        # were already here (isomp4, matroska, ogg, vorbis, opus,
        # flac, mpg123, openh264, libav, vpx — verified 2026-08-03).
        # X11 apps under the Wayland session (gnome-shell optdepends).
        xorg-xwayland
        # Fonts: GNOME's UI face + broad text coverage (incl. Noto Sans
        # Mono for gnome-console). Without any font the shell renders
        # boxes. CJK variants deferred (size).
        cantarell-fonts
        noto-fonts
        # PipeWire session manager: pulls pipewire and makes it start;
        # mutter/portal ScreenCast (§5.7.4 groundwork) and audio need a
        # running session manager, not just the libraries.
        wireplumber
        # alsa-utils: speaker-test/aplay/amixer — the only way to prove
        # sound at the driver layer, below PipeWire. Issue #44 was
        # precisely a case where every session-level indicator was green
        # and the hardware was mute; without these there is no way to
        # tell those two apart on the device (ADR-0024).
        alsa-utils
        # Input method (PLAN §5.7.3 layer 2). fcitx5 hosts the lisa-ime
        # addon: writing tools in any app, and the double-tap-Shift
        # summon. The -gtk/-qt modules are not optional in practice —
        # without them fcitx5 runs and no application ever routes a key
        # to it, which reads as the addon being broken rather than as a
        # missing toolkit bridge.
        fcitx5
        fcitx5-gtk
        fcitx5-qt
        # So the trigger key and the gesture can be changed or turned
        # off without editing a config file by hand.
        fcitx5-configtool
        # Wi-Fi from the desktop UI: gnome-shell's network indicator
        # only speaks NetworkManager (libnm is a gnome-shell dep; the
        # daemon is not). Runs with wifi.backend=iwd — iwd stays the
        # supplicant (conf in mkosi.extra); NM owns all interfaces,
        # see mkosi.postinst.chroot.
        networkmanager
        # Dart SDK (official): the Forge/LisaCode harness can run `dart
        # analyze`/`dart test` over a pubspec project (§5.12.1). Full
        # Flutter was never baked (ADR-0023), and the lane that fetched
        # it was removed on 2026-08-07 (ADR-0047 amendment) — issue #48
        # tracks re-scoping dart itself.
        dart
        # git: named here for the removed Flutter lane's aarch64 SDK
        # checkout (ADR-0027; lane removed 2026-08-07, ADR-0047
        # amendment). Kept because an immutable root cannot pacman it in
        # later and developer trees on the device expect it — re-evaluate
        # alongside dart (#48).
        git
        # mbsync — the IMAP→Maildir syncer the Mail app reads behind
        # (apps/mail/README.md "Sync is somebody else's job"). Lisa does
        # not grow its own mail network client: an OS whose defining
        # constraint is egress control has no business writing one when
        # a mature syncer exists. But "somebody else" has to actually be
        # on the disk — an immutable root cannot pacman it in later, and
        # a README telling the user to run mbsync on a system with no
        # mbsync is the same defect as documenting an unbuilt feature.
        isync
        # msmtp — the other direction. Reply and Forward are shown
        # disabled in the Mail app because sending needs SMTP, and a
        # button that does nothing reads as broken while a missing one
        # reads as unfinished.
        #
        # It implements XOAUTH2 itself rather than through Cyrus SASL,
        # so unlike mbsync it needs no plugin: `passwordeval` runs
        # `lisa mail token` and msmtp does the rest. GOA reports
        # SmtpAuthXoauth2=true and SmtpAuthLogin=false for the connected
        # Google account, so this is the only mechanism that would work
        # anyway.
        #
        # Same reasoning as isync: Lisa does not grow its own mail
        # network client, and "somebody else's job" only works when
        # somebody else is on the disk — an immutable root cannot add
        # one later.
        msmtp

[Validation]
# Signed UKI + SecureBoot keys wired in once the M0 boot test is green.
SecureBoot=no
