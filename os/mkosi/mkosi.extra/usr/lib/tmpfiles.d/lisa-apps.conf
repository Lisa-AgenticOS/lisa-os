# The app-update channel's state (ADR-0020): versioned interpreted app trees
# + the `current` symlink, on the persistent /var. Group-writable so
# `lisa apps update` works from the desktop user (lisa group) without root.
d /var/lib/lisa       0755 root root -
d /var/lib/lisa/apps  2775 root lisa -
